#Spring properties
spring:
  banner:
    location: classpath:bannerex/banner.txt
    
  datasource: 
    url: jdbc:mysql://localhost:3306/mybackend
    username: admin
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQLDialect
  thymeleaf:
    cache: false
